version: "3.7"
services:
  api:
    env_file:
      - ./env/.env
    build: .
    volumes:
      - .:/usr/app
    ports:
      - "${SERVER_PORT}:${INTERNAL_SERVER_PORT}"
    extra_hosts:
      - "${HOST_NAME}:${HOST_IP}"
